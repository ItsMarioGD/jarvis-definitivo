# Dependencias para Jarvis - Asistente Omnimodal
# Instalar con: pip install -r requirements.txt

# Framework principal
pipecat[all]>=0.1.0

# Servicios ElevenLabs (incluido en pipecat[all] pero explicitado)
elevenlabs>=1.0.0

# Servicios Qwen para Pipecat
pipecat-ai[qwen]>=0.1.0

# Opcional: Para modelo local Qwen via Ollama/vLLM
# ollama>=0.1.0

# Para transportes WebRTC
aiortc>=1.0.0
aiohttp>=3.8.0

# Utilidades de entorno
python-dotenv>=1.0.0
requests>=2.31.0
psutil>=5.9.0
customtkinter>=5.2.0

# Dependencias para Jarvis Core local (UI Funcional)
openai>=1.12.0
SpeechRecognition>=3.10.0

# Audio processing (paralingüística, STT local)
librosa>=0.10.0
numpy>=1.24.0

# TTS Local (Piper)
piper-tts>=1.2.0

# NOTA: Si solo quieres chatear con Qwen local, ejecuta:
#   pip install -r requirements-min.txt
# Esto evita ~300 MB de pipecat/aiortc que no se usan en Fase 1.

# Dependencias opcionales del generador (jarvis_generator.py):
# numpy, matplotlib, numpy-stl, openpyxl, python-pptx, python-docx, reportlab
# Se cargan perezosamente: si faltan, el generador reporta error especifico.

# Interfaz web Flask (solo si vas a usar web_interface/):
flask>=2.3.0
flask-socketio>=5.3.0

# MCP Servers
aiohttp>=3.8.0
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.0

# Mem0 (memoria tripartita)
mem0ai>=0.1.0

# Headroom (compresión contexto) — paquete interno NO publicado en PyPI.
# El import en jarvis_core.py está protegido con try/except, así que la app
# funciona sin esta dependencia (se omite la compresión de contexto).
# Si tienes acceso al paquete interno, instálalo manualmente:
#   pip install -e /ruta/a/headroom
# o coloca el código en _external/headroom y descomenta la línea de abajo:
# headroom @ file:///_external/headroom
